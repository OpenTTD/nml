grf {
    grfid: "test";
    name: string(STR_REGRESSION_NAME);
    desc: string(STR_REGRESSION_DESC);
    version: 0;
    min_compatible_version: 0;
}

// disable all trains
disable_item(FEAT_TRAINS);

// disable industry with ID 12
disable_item(FEAT_INDUSTRIES, 12);

// disable cargos 10 to 12
disable_item(FEAT_CARGOS, 10, 12);
